<article class="post">
    <div class="post-title">
    <p>{{title}}</p>
    </div>
    <div class="meta">
    Created by {{user.username}} on
    {{format_date created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" commments.length}}</a>
    </div>
</article>